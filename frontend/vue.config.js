module.exports = {
    //To make lint errors show up in the browser overlay, you can use lintOnSave: 'default'-> we have it set to false and therefore they will not show up
    lintOnSave: false,
    devServer: {
        port: 8081
    }
}
